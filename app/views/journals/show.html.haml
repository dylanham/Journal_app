.page-header
  .pull-right
    = link_to 'Edit Journal', edit_journal_path(@journal), class: 'btn btn-primary'
    = link_to 'Delete', journal_path(@journal), method: :delete, class: 'btn btn-danger'
  %h1= @journal.name
= link_to 'Create an Entry', new_journal_journal_entry(@journal)
